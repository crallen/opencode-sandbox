---
description: Create a well-formed Conventional Commits message from staged changes
agent: git-manager
subtask: true
---

Review the currently staged changes and create an appropriate commit following the Conventional Commits specification.

Current staged changes:
!`git diff --staged --stat`

!`git diff --staged`

Current branch:
!`git branch --show-current`

$ARGUMENTS
